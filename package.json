{
  "name": "borrowbridge-monorepo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend run start\"",
    "start": "concurrently \"npm --prefix frontend run dev\" \"npm --prefix backend run start\"",
    "build": "npm --prefix frontend run build"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^16.6.1",
    "mongoose": "^8.24.3"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
